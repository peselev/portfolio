---
export interface Props {
  label?: string;
}

const { label } = Astro.props;
---

<div class="case-prompt">
  {label && <p class="case-prompt-label">{label}</p>}
  <pre class="case-prompt-body"><slot /></pre>
</div>

<style is:global>
  .case-prompt {
    background: #f6f8fa;
    border: 1px solid #e5e7eb;
    border-radius: 0.5rem;
    padding: 1rem 1.25rem;
    margin: 1.25rem 0;
  }
  .case-prompt-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
    color: #6b7280;
    margin: 0 0 0.5rem 0;
  }
  .case-prompt-body {
    background: transparent;
    border: none;
    padding: 0;
    margin: 0;
    font-family: var(--font-mono);
    color: #111827;
    white-space: pre-wrap;
    word-break: break-word;
  }
  /* MDX wraps slot content in <p>, so target whatever lives inside <pre>.
     Reset prose-case paragraph styles and apply Prompt sizing. */
  .case-prompt-body p,
  .case-prompt-body * {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    line-height: 1.6;
    color: #111827;
    margin: 0;
    padding: 0;
  }
</style>
